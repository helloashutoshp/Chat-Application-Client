import { Menu } from '@mui/material'
import React from 'react'

const FileMenu = ({anchorE1,open,close}) => {
  return (
   <Menu open={open} anchorEl={anchorE1} onClose={close}>hhggh</Menu>
  )
}

export default FileMenu
